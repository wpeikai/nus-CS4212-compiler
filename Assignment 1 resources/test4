class MainTest {
    void main (Int a, String b) {
        println(new Fac().computeFac(a)) ;
        return b;
    }
}

class ConcatStrinfs {

    String a;
    String concat(String b) {
        String res;
        res = "%s%s";
        return res.format(a, b);
    }
}